# SalesDeliveryBI — Frontend Implementation Checklist

Phased build order, mirrors `docs/plans/backend/checklist.md`. Each phase should be functionally complete and buildable before moving to the next — don't skip ahead with stubs. Follows `docs/plans/frontend/architecture.md` exactly; flag any deviation before implementing.

---

## Phase 0 — Project Setup

- [x] `ng new` Angular 22 app in `src/frontend` — standalone components, signals, no NgModule scaffolding (Angular CLI 22.1.2; required bumping local Node.js from v24.14.1 → v24.18.1 LTS via winget, confirmed with the user first since it's a system-wide install)
- [x] Add AG Grid Community, PrimeNG, `ng-apexcharts` — verify peer-dep compatibility with Angular 22 first; pin to the highest version all three support if one lags (`ag-grid-angular`/`ag-grid-community` 36.0.2, `primeng` 22.0.0 + `@angular/cdk` 22.1.0, `ng-apexcharts` 2.4.0 + `apexcharts` pinned to 5.16.0 — latest `apexcharts` is 6.x but `ng-apexcharts`'s peer range is `^5.10.3`, so 6.x was deliberately avoided)
- [x] Add PrimeNG theme + icon set; confirm it doesn't fight AG Grid's theme — `providePrimeNG({ theme: { preset: Aura } })` wired in `app.config.ts` via `@primeuix/themes`; icons come from `@primeicons/angular` (component-based, bundled as a `primeng` dependency, no separate CSS import needed); also added `@angular/animations` + `provideAnimationsAsync()` — PrimeNG overlay components still need it even though the classic Animations API is deprecated in v22. AG Grid 36 uses the JS Theming API (no global CSS), so the actual grid theme (e.g. `themeQuartz` tuned to match Aura) is picked when the first grid component is built in Phase 3/4, not here.
- [x] `.editorconfig` / ESLint / Prettier config — `.editorconfig`/`.prettierrc` scaffolded by `ng new`; added `@angular-eslint/schematics` via `ng add` for `eslint.config.js` + the `lint` architect target
- [x] `environment.ts` / `environment.development.ts` — generated via `ng generate environments`; `apiBaseUrl` set to `/api` (prod, same-origin/reverse-proxy) and `https://localhost:7041/api` (dev, matches the backend's `launchSettings.json` HTTPS profile)
- [x] Create folder structure per `architecture.md` — `features/quotation-pipeline`, `features/quotation-conversion`, `features/quotation-aging` created now (each holding a minimal real page component, needed so the router has something to lazy-load); `core/`'s and `shared/`'s deeper subfolders (`auth`, `http`, `models`, `components`, `charts`, `pipes`, `data`) are intentionally **not** pre-created empty — per `CLAUDE.md`'s "no placeholders" rule they're created with real content in Phase 2/3 instead. Added `core/not-found/not-found.page.ts` (not in `architecture.md`, needed for the wildcard route below).
- [x] `app.routes.ts` — 3 lazy routes (`loadComponent`) for pipeline/conversion/aging, default redirect `''` → `pipeline`, wildcard `**` → the not-found page
- [x] Confirm `ng build` and `ng serve` both run clean on the empty scaffold before adding features — `ng lint` clean, `ng build --configuration production` produces one small initial chunk + 4 separate lazy chunks (confirms per-route code-splitting from day one), `ng test` passes, and all 4 routes (`/pipeline`, `/conversion`, `/aging`, unknown → not-found) verified live via `ng serve`. Also stripped the default Angular marketing template from `app.html`/`app.ts`/`app.spec.ts` down to a bare `<router-outlet />` shell — the real layout lands in Phase 1.

- [x] **Post-Phase-4 correction: downgraded Angular 22 → 21, PrimeNG 22 → 21.** While restyling the shell (dark sidebar/light content admin look, requested after Phase 4), the app started rendering a permanent "Invalid PrimeUI License" banner — PrimeNG 22.0.0 turned out to have introduced a real paid-license requirement (`@primeui/license-manager`, `providePrimeNG({ license: ... })`, free "Community License" tier available but requires registering a key at primeui.dev). Confirmed via `npm view primeng@21.1.9 peerDependencies` that the last pre-license PrimeNG version needs `@angular/core ^21.0.7` — there is no PrimeNG version that is both pre-license and Angular-22-compatible. Discussed 3 options with the user (get a free license key / downgrade to Angular 21 / drop PrimeNG for an alternative); user chose the downgrade. Confirmed AG Grid (`ag-grid-angular@36.0.2`, peer `>= 20.0.0`) and `ng-apexcharts@2.4.0`/`apexcharts@5.16.0` (peer `>=20.0.0`) both accept Angular 21 unchanged, so only the Angular/PrimeNG packages moved: `@angular/*` → `^21.2.19` (`@angular/cdk` → `^21.2.14`, latest non-prerelease), `primeng` → `^21.1.9`, `@primeuix/themes` → `^2.0.3` (matches `primeng@21`'s `@primeuix/styled ^0.7.4` dependency, vs. `^3.0.0` for the v22 line), `typescript` → `~5.9.3` (`@angular/build@21`'s peer range is `>=5.9 <6.0`, excluding the previously-pinned `~6.0.2`), `angular-eslint` → `21.4.0`. One new explicit dependency: `@primeicons/angular@^8.0.0` — PrimeNG 22 depended on it transitively, PrimeNG 21 doesn't depend on it at all (v21 never needed it, since per-icon Angular components were a v22-era addition), so without adding it directly every icon import (`@primeicons/angular/bars`, `/user`, `/arrow-up`, etc.) 404'd at build time. Full clean reinstall (`rm -rf node_modules package-lock.json && npm install`) was required — `npm install` alone hit `ERESOLVE` conflicts against the stale v22 packages still on disk. Re-verified after the downgrade: `ng build --configuration production` clean, `ng lint` clean, all 35 existing unit tests still pass unmodified, and the Pipeline dashboard re-tested live end-to-end (KPIs/grid/status-badges/currency-day formatting, no console errors, license banner gone).

## Phase 1 — App Shell / Layout (professional, responsive)

- [x] `layout/shell.ts` wrapping every dashboard route (nested under a parent route with `component: Shell` + children, not `app.ts` itself) — CSS Grid: topbar + sidebar + content + footer, not ad-hoc per-page layout. `app.html`/`app.ts` stripped down to a bare `<router-outlet />` — the default Angular marketing template is fully gone.
- [x] **Topbar** (`layout/topbar`): brand (left) + hamburger (mobile only), unit indicator + user avatar/menu (right); sticky (`position: sticky; top: 0`), fixed `--topbar-height: 64px`, no layout shift. `unitLabel` defaults to `'All Units'` (the real semantic default for a null/unset `unitId` per the API contract, not a fake placeholder) and `userDisplayName` defaults to `null` (renders a generic user icon) — both are component inputs Phase 2's `core/auth` will bind to real values; Topbar itself is fully functional today in the not-yet-authenticated state. **Correction from Phase 3**: `<p-toolbar styleClass="topbar">` was a no-op — `Toolbar` has no `styleClass` input — so the height/border/sticky CSS below never actually applied until Phase 3 caught it and switched to a plain `class="topbar"` attribute (which always works). Confirmed live afterward.
- [x] **Sidebar** (`layout/sidebar`): nav links to Pipeline/Conversion/Aging with icons (`@primeicons/angular`) + `routerLinkActive` highlighting; a single `<ng-template #navList>` is reused via `NgTemplateOutlet` in both the static aside (tablet rail/desktop full) and the mobile drawer, so the nav markup isn't duplicated. Verified: hidden `<768px` (drawer only), 64px icon-only rail at 768–1023px, 240px full-label at 1024px+, no horizontal scroll at any width.
- [x] **Content area**: `<main class="shell__content">` + `<router-outlet />`; consistent `max-width: 1440px` + padding across all 3 dashboard routes (page-title/breadcrumb region deferred to each dashboard page in Phase 4-6, since there's no real page title content yet).
- [x] **Footer** (`layout/footer`): app name + version sourced from `environment.version` (not a hardcoded string) — centered, stacks fine on mobile, never overlaps content (`grid-area: footer`, own row).
- [x] Use PrimeNG layout primitives (`Toolbar`, `Drawer`) for topbar/sidebar — **deviation**: `Menu` (popup) is NOT used for the user-menu dropdown. PrimeNG 22.0.0's `Menu` has a confirmed upstream bug (`parentId_r3 is not defined`, thrown from its own compiled `@for` track function inside the recursive-items template — reproduced with a single flat `MenuItem`, unrelated to our usage). Replaced with a small hand-rolled dropdown (`signal` + `@HostListener('document:click'/'document:keydown.escape')` to close on outside-click/Escape) — revisit and switch back to `p-menu` once PrimeNG ships a patch past 22.0.0. The sidebar's nav list is also intentionally plain `<a routerLink>` elements rather than PrimeNG `Menu`'s `MenuItem[]` model — simpler and gives correct `routerLinkActive` highlighting directly, with no generic-model indirection for 3 static links.
- [x] Global loading indicator — `core/http/loading-indicator.ts` (`LoadingIndicatorService` + `loadingIndicatorInterceptor`, a pending-request counter registered via `provideHttpClient(withInterceptors([...]))` in `app.config.ts`); Shell renders a fixed top progress bar whenever `isLoading()` is true. Real today even though nothing calls the backend yet — Phase 2/4-6's HTTP calls will make it visible.
- [x] Global 403 (`core/not-authorized`, routed at `/403`, own layout — verified no topbar/sidebar render on it) and 404/not-found (`core/not-found`, wildcard route, own layout, link back to `/pipeline`) — both outside the `Shell` route so they never inherit dashboard chrome.
- [x] Verified the shell at mobile (375px), tablet (768px), desktop (1280px) via live `ng serve` + browser DOM/computed-style checks: sidebar rail/drawer/full-width switch correctly, hamburger only shows <768px, user-menu dropdown opens/closes (click + outside-click + Escape), active-link highlighting, zero horizontal scroll at any breakpoint, zero console errors. Raised the initial-bundle warning budget in `angular.json` (500kB → 700kB) to reflect the real cost of Toolbar/Drawer/Avatar/animations — actual initial chunk is 572kB, still well under the 1MB hard error.

## Phase 2 — Core Services

- [x] `core/auth` — `jwt.ts` (base64url decode + `exp` check, no signature verification client-side — that's the backend's job), `current-user.service.ts` (`CurrentUserService`: `sub`/`permissions`/`userUnits`/`isAuthenticated` signals read from `localStorage['sdbi_auth_token']`, `setToken`/`clearToken`/`hasPermission`), `auth.guard.ts` (`CanActivateFn`, applied via `canActivate: [authGuard]` on the `Shell` route in `app.routes.ts`). **Known limitation, called out explicitly**: the Identity service that issues these tokens doesn't exist yet (external dependency, same open item as the backend checklist's Phase 7) and login is out of scope for this repo per `CLAUDE.md` §8 — so today the guard redirects an unauthenticated visitor to `/403` (there's no distinct "please log in" page to send them to instead). Revisit once the Identity service defines its actual login/redirect flow. `Topbar`'s `unitLabel`/`userDisplayName` inputs are still unbound (Shell doesn't pass them yet) — there's no unit-name lookup (Phase 3) or a `name` claim in the JWT contract to bind to.
- [x] `core/http` — `auth.interceptor.ts` (attaches `Authorization: Bearer` only to requests whose URL starts with `environment.apiBaseUrl`), `error.interceptor.ts` (403 → `router.navigateByUrl('/403')`, never a toast; 404/500 → `MessageService.add(...)`, never a redirect). Registered in `app.config.ts` as `withInterceptors([loadingIndicatorInterceptor, errorInterceptor, authInterceptor])` — `errorInterceptor` first (outermost) so its `catchError` sees failures from the whole chain; `MessageService` provided at the root (`app.config.ts`) so the interceptor and the single `<p-toast>` in `app.html` share the same instance. `Toast`'s `@for` loop tracks by `msg` directly (no nested-context variable), unlike the broken `Menu` from Phase 1 — verified working live.
- [x] `core/models` — genuinely **generated**, not hand-mirrored: started the backend (`dotnet run`, against the already-running dev Postgres/Redis containers), fetched the live `/openapi/v1.json`, ran `openapi-typescript` against it → `api-schema.d.ts` (git-ignored from lint via `eslint.config.js`, regenerate with `npm run generate:api-types`). `dashboard.models.ts` re-exports typed aliases (`QuotationPipelineDto`, `ConversionDto`, `AgingDto`, etc.) plus the shared `DashboardResponse<T>` envelope — zero hand-typed fields, so no drift risk. This caught a real detail `api-contract.md` doesn't mention: numeric fields are typed `number | string` (System.Text.Json's OpenAPI schema for C# `decimal`), which the hand-written contract doc glosses over as plain numbers.
- [x] `shared/data/query-signal.ts` — `createQuerySignal(fetchFn)` returns `{ data, lastRefresh, loading, error, load() }`; internally a `Subject` piped through `switchMap` (cancels a still-in-flight request when `load()` is called again before it resolves) + `takeUntilDestroyed(inject(DestroyRef))`. Must be called from an injection context (e.g. a feature service's field initializer) — documented in a JSDoc comment since it's a non-obvious constraint. Verified with a dedicated spec: an older in-flight response arriving after a newer `load()` call is confirmed discarded (`query-signal.spec.ts`).
- [x] Confirmed interceptor JWT attachment two ways: (1) `auth.interceptor.spec.ts` via `HttpTestingController` — header present for `environment.apiBaseUrl`-prefixed requests with a token, absent with no token, absent for a non-API request (`/assets/logo.png`) even with a token set; (2) live end-to-end — minted a real dev-signing-key JWT (matching `appsettings.Development.json`) and hit the running backend directly with `curl`: own-unit request → `200` with real KPI data, foreign-unit request → `403` Problem Details, no token → `401`. Also verified live in the browser: no token in `localStorage` → `/pipeline` redirects to `/403`; valid token set → `/pipeline` loads. `error.interceptor.spec.ts` covers the 403/404/500 branching. 17/17 tests pass, `ng lint` clean, `ng build --configuration production` clean (468kB initial, back under the 500kB budget after Phase 1's `p-menu` removal).

## Phase 3 — Shared Components

- [x] `kpi-card` — `label`/`value` inputs + optional `trend: { direction, sentiment, label }` — `sentiment` (good/bad/neutral) is independent of `direction` (up/down/flat) on purpose, since a rising "lost value" is `direction: up` but `sentiment: bad`; color follows sentiment, arrow follows direction. `:host` is `flex: 1 1 180px; min-width: 160px`, so it wraps/stacks correctly inside any `flex-wrap` row a dashboard page builds in Phase 4-6 — no separate "kpi-row" component, since architecture.md only calls for the reusable card.
- [x] `data-as-of` — takes `lastRefresh: string | null` and renders "Data as of {date}" via `DatePipe`, or "Data as of —" when null; zero client-side computation of the timestamp itself.
- [x] `unit-date-filter` — unit dropdown (`p-select`) built from `CurrentUserService.userUnits()` (the `user_units` claim) plus a leading "All Units" (`unitId: null`), never an open/unfiltered list; two `p-datepicker`s for the date range, hidden via `showDateRange = input(true)` (Aging passes `false`); all field changes funnel through one `Subject` + `debounceTime(300)` before emitting `filterChange`, verified in `unit-date-filter.spec.ts`. **Unit display names**: there is no units-lookup endpoint anywhere in the API contract — the JWT only carries GUIDs. Asked the user; went with a dev-only GUID→name map (`dev-unit-catalog.ts`, matching `docs/plans/database/seed-data.md`, same category of stand-in as the backend's dev-only JWT signing key) that falls back to the raw GUID for anything not in it. Deliberately did **not** implement the `bi.quotation.viewAllUnits` nuance (a user with that permission can query units outside their `user_units`) — out of scope for what architecture.md actually specifies; flagged here for a future revisit once a real units catalog exists.
- [x] `status-badge` — `@switch` on `status()` rendering a distinct icon per value (Draft/Submitted/Negotiation/PendingApproval/Approved/Converted/Rejected/Expired + a `circle` fallback), color from `STATUS_BADGE_COLOR`. Colors follow the dataviz skill's collision rule: the three true good/bad terminal outcomes (Converted/Rejected/Expired) wear the reserved status tokens (good/critical/serious); the in-progress workflow stages use categorical/neutral tones instead, since they aren't a good/bad outcome.
- [x] `apex-chart-theme.ts` — palette sourced from the dataviz skill's validated reference palette (`references/palette.md`), confirmed with `validate_palette.js` (categorical passes with a contrast WARN requiring visible labels — satisfied by direct/legend labels in every chart; the 5-step aging-bucket ordinal ramp passes `--ordinal` outright). Won/lost wears status good/critical (a real good/bad pair, not "series 4", per the skill's collision rule). Light-mode only — this app has no dark-mode requirement anywhere else, so the ramp-inversion complexity for a dark surface wasn't built.
- [x] `currency-usd`, `days-open` pipes — both normalize the API's `number | string` decimal quirk (see Phase 2's generated-DTOs note) before formatting, so no feature component needs to know about that quirk itself.
- [x] `loading-skeleton` (PrimeNG `Skeleton`) — configurable KPI-row placeholder count + an array of content-block heights, reused by all 3 dashboards' pages in Phase 4-6 instead of each hand-rolling its own.
- [x] **Two real bugs found and fixed**, the same class of mistake as Phase 1's `Menu` discovery but this time in *my own* code, not PrimeNG's: `Toolbar`, `Select`, and `DatePicker` have **no `styleClass` input** (only some PrimeNG components do — `Drawer` does, confirmed by grepping each one's actual `.d.ts` before trusting it). `topbar.html`'s `<p-toolbar styleClass="topbar">` and `unit-date-filter.html`'s `styleClass="unit-date-filter__unit"/"__date"` were silently no-ops — the custom CSS never applied; the elements only ever got PrimeNG's own default styling. Fixed by using the plain `class="..."` attribute instead (which always works, component-specific or not) — confirmed live afterward (`topbar` height/border/sticky and the unit/date field widths all measured correctly via `getComputedStyle` in the browser). Also caught before it shipped: a dynamically-bound `[attr.data-p-icon]="expr"` does **not** activate a `@primeicons/angular` icon component, since Angular matches component selectors against literal template attribute values at compile time, not runtime bindings — `status-badge` and `kpi-card`'s trend icon both use `@switch` with a literal `data-p-icon="..."` per branch instead.
- [x] Verified everything together live: temporarily wired all 6 shared components into the `pipeline.page.ts` placeholder, ran `ng serve`, drove it with real DOM interaction (opened the unit dropdown, clicked an option, opened a date picker, clicked a day) — confirmed the debounced `filterChange` emits exactly once per settled interaction with the correct ISO date/unit-GUID shape, zero console errors — then reverted the placeholder back to its Phase 0 state. 35/35 unit tests pass (11 new specs this phase), `ng lint` and `ng build --configuration production` both clean.

## Phase 4 — Feature: Quotation Pipeline

- [x] `pipeline.service.ts` — `PipelineFilter { unitId }` on `query-signal.ts`, `GET {apiBaseUrl}/sales/quotations/pipeline` via `HttpClient` (`unitId` omitted from `HttpParams` entirely when null, matching the contract's "omitted means all assigned units"). `providedIn: 'root'`, same singleton pattern as `CurrentUserService`. **Note**: unlike Conversion, the pipeline endpoint takes no `fromDate`/`toDate` per `api-contract.md` — the page passes `[showDateRange]="false"` to `unit-date-filter` and the service only ever reads `unitId` off the emitted value.
- [x] `pipeline-grid.component.ts` — first AG Grid wiring in this repo (`ModuleRegistry.registerModules([AllCommunityModule])`, `themeQuartz.withParams(...)` sourced from `apex-chart-theme.ts`'s ink/font tokens for visual consistency with the rest of the app, since no AG Grid pattern existed yet to follow). Columns: `quotationNo`/`buyerName` (always visible), `status` (renders `StatusBadge` via a small `ICellRendererAngularComp` wrapper, `PipelineStatusCellRenderer`, so the grid reuses the same icon+color logic as everywhere else instead of re-implementing it), `valueUsd`/`daysOpen` (formatted through the existing `CurrencyUsdPipe`/`DaysOpenPipe` instances inside `valueFormatter`, not new formatting logic). `merchandiserName` and `daysOpen` are the low-priority columns, hidden below 768px via `gridApi.setColumnsVisible(...)` on `gridReady` + a `window:resize` listener — verified live (see below) rather than shrunk illegibly.
- [x] `pipeline.page.ts` / `.html` — KPI row (`Open Quotations`, `Pipeline Value`, `Pending Approval`, `Avg. Days Open`, the two counts normalized through a small local `asCount()` since `KpiCard.value` is `string` but the DTO's counts are `number | string`) + `unit-date-filter` + `pipeline-grid`, inside the Phase 1 `shell__content` area. Initial load fires from the constructor (`service.load({ unitId: null })`) since `unit-date-filter` only emits on a user-driven change, not on init.
- [x] Loading skeleton (`app-loading-skeleton`), an explicit error block (`service.error()`) if the request fails, and an empty-state block (`openQuotations().length === 0`) once data loads with zero rows — all three are distinct `@else if` branches in `pipeline.page.html`, never a blank screen.
- [x] Verified live end-to-end via `ng serve` + browser: `ng build --configuration production` (pipeline-page chunk isolated at 1.42MB/299kB transfer — AG Grid's weight confirmed NOT in the shared initial bundle) and `ng lint` both clean. Drove the real page with a dev-minted JWT (no signature check client-side, same approach as Phase 2) against a temporary mock endpoint standing in for the not-yet-running backend: confirmed the loading skeleton, the error state (real `ERR_CONNECTION_REFUSED` against the actual dev `apiBaseUrl` before the mock was pointed to), the populated KPI/grid state with correct currency/day/status formatting, the empty-state message after switching to a unit with zero rows, and responsive column hiding (`Merchandiser`/`Days Open` confirmed hidden at 375px mobile, both confirmed present at 768px/1280px with the grid scrolling horizontally in its own container, never the page). `environment.development.ts` reverted back to the real backend URL afterward — the mock was scratch-only, not committed.

## Phase 5 — Feature: Conversion & Win/Loss

- [x] `conversion.service.ts` — `ConversionFilter { unitId, fromDate, toDate }` on `query-signal.ts`, `GET {apiBaseUrl}/sales/quotations/conversion` (all 3 params optional `HttpParams`, matching the contract's `unitId?`/`fromDate?`/`toDate?`, default "current month" is entirely server-side). Regenerated `api-schema.d.ts` from the now-running live backend (`openapi-typescript` against a locally-saved `openapi/v1.json`, since the CLI's own HTTP fetch failed on the dev cert's self-signed chain) — came back byte-identical to Phase 2's version, confirming `ConversionDto`/`ConversionKpisDto`/`MonthlyTrendEntryDto`/`BuyerPerformanceDto` were already correctly typed.
- [x] `conversion-trend-chart.component.ts` (ApexCharts line, `apx-chart` full bundle) — conversion-rate-by-month, y-axis capped at 100% with a `%` formatter, on `apex-chart-theme.ts` tokens. `win-loss-chart.component.ts` (ApexCharts bar) — **deviation from the architecture doc's "won vs lost value by month" wording**: the API's `ConversionKpisDto` only exposes an aggregate `wonValueUsd`/`lostValueUsd` for the whole filtered period, no per-month won/lost breakdown (`monthlyTrend` only carries `conversionRatePct`) — so this is a 2-bar Won-vs-Lost comparison for the current filter, not a time series; documented inline in the component. Both use status good/critical colors for won/lost (a real good/bad pair, consistent with `status-badge`'s color rule).
- [x] `buyer-performance-grid.component.ts` — AG Grid, columns `buyerName`/`conversionRatePct`/`valueUsd` always visible, `quotationsCount`/`wonCount`/`lostCount` hidden below 768px (same responsive pattern as Phase 4's pipeline grid). **Refactor**: extracted the AG Grid theme + `ModuleRegistry.registerModules(...)` call out of `pipeline-grid.component.ts` into a new `shared/ag-grid/ag-grid-setup.ts` (`AG_GRID_THEME`) — now genuinely shared by 2 grids instead of copy-pasted, so the two dashboards' grids can't visually drift apart.
- [x] `conversion.page.ts` — KPI cards (Conversion Rate, Won Value, Lost Value, Avg. Conversion Time) + `unit-date-filter` (default `showDateRange=true`, unlike Pipeline) + the 2 charts (`grid-template-columns: repeat(auto-fit, minmax(320px, 1fr))`, stacks on mobile) + buyer grid. New `shared/pipes/rate-percent.pipe.ts` (`ratePercent`) added alongside the existing `currencyUsd`/`daysOpen` pipes — same `number | string` normalization pattern, used by both the KPI card and the grid's `conversionRatePct` column (named to avoid colliding with Angular's built-in `PercentPipe`).
- [x] Loading skeleton, explicit error block, and an empty-state message (`buyerPerformance().length === 0`) — same 3-branch pattern as Pipeline.
- [x] **Connected the frontend to the real (not mocked) backend for the first time** — started the actual `SalesDeliveryBI.Api` (`dotnet run --launch-profile https`) against the already-running dev Postgres/Redis containers, and `ng serve`. Found and fixed a real gap neither Phase 2's `curl`-based verification nor Phase 4's mock-server verification could have caught: the backend had **no CORS policy at all**, so every browser request from `ng serve`'s origin (`http://localhost:4200`) failed the preflight (`OPTIONS → 405`) even though `curl` (which doesn't enforce CORS) worked fine. Fixed in `Program.cs` — a `Development`-only CORS policy allowing `http://localhost:4200`/`https://localhost:4200`, mirroring the existing dev-only-signing-key pattern; production still serves the frontend same-origin (`apiBaseUrl: '/api'`) and registers no CORS policy at all. Verified end-to-end with a real HS256-signed JWT (matching `appsettings.Development.json`'s dev signing key and `docs/plans/security/security-plan.md`'s claim shape) for both dashboards: Pipeline showed the real seeded quotations; Conversion showed real KPIs/trend/buyer-performance for a June–July 2026 filter (`conversionRatePct: 15%`, `wonValueUsd: $237,000`, 5 buyers), matching a direct `curl` call to the same endpoint exactly. Confirmed no console errors and correct responsive column-hiding on the buyer grid at 375px mobile. `ng build --configuration production` and `ng lint` clean, all 35 existing tests still pass unmodified.

## Phase 6 — Feature: Aging

- [x] `aging.service.ts` — `AgingFilter { unitId }` on `query-signal.ts`, `GET {apiBaseUrl}/sales/quotations/aging` (unit-only filter, no date range per `architecture.md`; confirmed against the live backend's OpenAPI schema — the endpoint's query params really are `unitId?` only).
- [x] `aging-bucket-chart.component.ts` (ApexCharts bar) — the 5 buckets (`0-7`/`8-15`/`16-30`/`31-60`/`60+`) re-sorted client-side into a fixed order (the API doesn't guarantee array order), value = `valueUsd` per bucket, colored with `apex-chart-theme.ts`'s `agingOrdinalRamp` (the 5-step ramp built in Phase 3 specifically for this chart).
- [x] `aging-grid.component.ts` — AG Grid, `riskLevel` hidden below 768px (`quotationNo`/`buyerName`/`status`/`daysOpen`/`valueUsd` always visible).
- [x] `aging.page.ts` — KPI cards (Total Open Value, High-Risk Aged Value) + unit-only `unit-date-filter` (`[showDateRange]="false"`) + bucket chart + grid.
- [x] Loading skeleton, error block, empty-state message — same 3-branch pattern as Pipeline/Conversion.
- [x] **Found and fixed a real AG Grid + Angular 21 bug while building this phase**: `cellRenderer` (both `ICellRendererAngularComp` and a plain class implementing `ICellRenderer`) never mounted in either grid — cells stayed permanently blank with zero console errors, reproduced even with a trivial hardcoded-template renderer. Root-caused via direct runtime inspection (not guesswork): confirmed the colDef was correctly wired (`hasCellRenderer: true`), confirmed the renderer class worked perfectly when manually instantiated, and confirmed a **plain function** cellRenderer also failed on the grid's first render but any *later* re-application of the grid's own columnDefs fixed it — pointing at an AG Grid Angular 36 initialization-order bug, not a code bug. Rather than ship a timing-dependent `setTimeout` workaround, replaced the cellRenderer approach entirely with `valueFormatter` (plain text) + `cellStyle` (colored, via `shared/ag-grid/status-badge-cell-renderer.ts`'s `statusBadgeValueFormatter`/`statusBadgeCellStyle`) — the same proven-working code path already used for the currency/day columns in both grids. Trade-off: the grid's status cells are colored text, not the icon+pill `status-badge` component (which is unaffected and keeps its icons everywhere else it's used). Re-verified after the fix: Pipeline's status column (previously silently broken since Phase 5, when this cell-renderer pattern was first introduced) now also renders correctly. `ng build`, `ng lint`, all 35 tests, and a live check against the real backend all confirm the fix.

## Phase 7 — Cross-Cutting Verification

- [x] All 3 dashboards re-tested at mobile (375px)/tablet/desktop (1280px) against the live backend with real seeded data — Pipeline (9 open quotations, correct KPI/grid/status values), Conversion (real buyer-performance rows for a June–July 2026 range), Aging (aging-bucket chart + grid) all confirmed rendering correctly, zero horizontal page scroll at any breakpoint, low-priority columns (`merchandiserName`/`daysOpen` on Pipeline, `quotationsCount`/`wonCount`/`lostCount` on Conversion, `riskLevel` on Aging) correctly hidden below 768px.
- [x] Rapid filter changes confirmed to not race — fired 3 unit-date-filter changes back-to-back on Conversion (`2026-06`, `2026-07`, then `2026-06–07`); final displayed data matched only the *last* filter (`conversionRatePct: 15.38%`, same buyer-performance rows as a direct `curl` call for that exact range), confirming `query-signal.ts`'s `switchMap` correctly discarded the two superseded in-flight requests. Matches the existing `query-signal.spec.ts` unit test's coverage of the same behavior.
- [x] 403 end-to-end — confirmed the backend itself really does reject an out-of-scope unit: minted a JWT with `user_units` deliberately not containing a seeded unit, requested `GET /pipeline?unitId=<seeded-unit>` directly via `curl` → real `403` (not a client-side assumption). The frontend's `error.interceptor` unconditionally redirects any `403` to `/403` regardless of cause (already unit-tested in Phase 2, `error.interceptor.spec.ts`); confirmed the target page itself renders the distinct "Not authorized" copy (never a toast, never a blank grid) with its own layout (no topbar/sidebar). Note: the unit dropdown only ever offers the caller's own `user_units` by design (Phase 3), so a real out-of-scope request can only originate from something other than normal UI navigation — which is exactly why the server owning this check (not just the client) matters.
- [x] `data-as-of` confirmed to match the backend response's `lastRefresh` exactly — captured Pipeline's actual network response (`lastRefresh: "2026-08-04T03:36:00.010837Z"`) and compared against the rendered "Data as of Aug 4, 2026, 9:36:00 AM" — exact match. (A separate ad-hoc `fetch` moments later returned a *newer* `lastRefresh`, 3 minutes on — expected, since the MV cache had refreshed in between; not a bug, and a useful reminder that `data-as-of` must always read the specific response bound to what's displayed, never a fresh unrelated request.)
- [x] Lazy-loaded route bundles confirmed via `ng build --configuration production` output: `pipeline-page` (5.64kB), `conversion-page` (9.60kB), and `aging-page` (7.33kB) are each their own isolated lazy chunk — none of AG Grid's (~1.37MB) or ApexCharts' (~615-620kB ×2 variants + ~375kB core) weight is in the initial bundle or duplicated per-route; it loads once, lazily, the first time any route needing it is visited.
- [x] Accessibility pass: status badges were already color+text (never color-only, confirmed unaffected by the Phase 6 AG Grid `cellRenderer` bug fix — grid cells still show the literal status word, just as colored text instead of an icon+pill). Keyboard navigation confirmed by code review — every interactive element in the shell/filter bar is a native `<a>`/`<button>`/PrimeNG form control (no `div`+`(click)` without a semantic role), so keyboard support comes from the browser/PrimeNG/AG Grid, not custom code. **Found and fixed a real gap**: all 3 charts had `dataLabels: { enabled: false }` (the shared `apex-chart-theme.ts` default) — meaning chart values were reachable *only* via hover tooltip, exactly the anti-pattern this checklist item calls out. Enabled `dataLabels` on all 3 (`conversion-trend-chart`, `win-loss-chart`, `aging-bucket-chart`) with the same formatters already used for their tooltips (`%`/`$` — confirmed live: aging bucket chart now shows `$417,000`, `$94,000`, etc. directly on the bars, no hover needed).

## Phase 8 — Testing

- [ ] Component tests: `data-as-of`, global 403 state, `unit-date-filter` (claim-scoped options, debounce)
- [ ] Service tests: each feature service's `query-signal.ts` flow, including cancel-on-new-filter behavior
- [ ] AG Grid responsive column-state presets and ApexCharts `responsive` breakpoint configs — one test per dashboard per breakpoint

## Phase 9 — Hardening / Polish

- [ ] Production build (`ng build --configuration production`) — confirm no console errors, bundle sizes reviewed
- [ ] Lighthouse pass on all 3 dashboards (performance + accessibility) at desktop and mobile
- [ ] Final visual QA against the Phase 1 shell — consistent spacing/typography/palette across all 3 dashboards, no per-page style drift
